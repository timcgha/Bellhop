const fs=require('fs');
const path=require('path');
const file=path.join(__dirname,'browser-level5-enhancement-verify.js');
let s=fs.readFileSync(file,'utf8');
function rep(a,b,label){if(!s.includes(a))throw new Error('patch needle missing: '+label);s=s.replace(a,b);}
rep("const results={challengeBeats:4};",`const results={challengeBeats:4};
let activeCDP=null;
async function nativeSpace(down){
  if(!activeCDP)throw new Error('native Space requested before CDP ready');
  await activeCDP.send('Input.dispatchKeyEvent',{type:down?'keyDown':'keyUp',key:' ',code:'Space',windowsVirtualKeyCode:32,nativeVirtualKeyCode:32,text:down?' ':undefined,unmodifiedText:down?' ':undefined});
}
async function nativeTapSpace(ms=65){await nativeSpace(true);await sleep(ms);await nativeSpace(false);await sleep(70);}`, 'native Space helper');
rep("if(stuck>=6){await tap(ev,'Space',55);stuck=0;}","if(stuck>=6){await nativeTapSpace(55);stuck=0;}",'driveTo recovery jump');
rep("async function jumpForward(ev,ms=1050){await cameraForward(ev);await key(ev,'KeyW',true);await tap(ev,'Space',55);await sleep(300);await tap(ev,'Space',55);await sleep(Math.max(260,ms-420));await key(ev,'KeyW',false);await sleep(260);}","async function jumpForward(ev,ms=1050){await cameraForward(ev);await key(ev,'KeyW',true);await nativeTapSpace(55);await sleep(300);await nativeTapSpace(55);await sleep(Math.max(260,ms-420));await key(ev,'KeyW',false);await sleep(260);}",'natural quicksand jump');
rep("cdp=await openCDP();await viewport(cdp,844,390);", "cdp=await openCDP();activeCDP=cdp;await viewport(cdp,844,390);", 'activate native CDP');
rep("results.camel={};results.camel.south=await direction(cdp.evaluate,['KeyS'],'south',[0,1]);results.camel.north=await direction(cdp.evaluate,['KeyW'],'north',[0,-1]);results.camel.east=await direction(cdp.evaluate,['KeyD'],'east',[1,0]);results.camel.west=await direction(cdp.evaluate,['KeyA'],'west',[-1,0]);const q=Math.SQRT1_2;results.camel.diagonal=await direction(cdp.evaluate,['KeyW','KeyD'],'diagonal',[q,-q]);",`results.camel={};results.camel.south=await direction(cdp.evaluate,['KeyS'],'south',[0,1]);results.camel.north=await direction(cdp.evaluate,['KeyW'],'north',[0,-1]);results.camel.east=await direction(cdp.evaluate,['KeyD'],'east',[1,0]);results.camel.west=await direction(cdp.evaluate,['KeyA'],'west',[-1,0]);const q=Math.SQRT1_2;results.camel.diagonal=await direction(cdp.evaluate,['KeyW','KeyD'],'diagonal',[q,-q]);
    await cameraForward(cdp.evaluate);await key(cdp.evaluate,'KeyW',true);await sleep(280);const turnYaw0=await cdp.evaluate('__P.yaw');await key(cdp.evaluate,'KeyD',true);await sleep(460);const turn=await cdp.evaluate(\`(()=>{const c=__P.camel;c.g.updateMatrixWorld(true);const root=new THREE.Vector3(),nose=new THREE.Vector3();c.g.getWorldPosition(root);c.g.userData.neck.getWorldPosition(nose);let nx=nose.x-root.x,nz=nose.z-root.z,nl=Math.hypot(nx,nz)||1;nx/=nl;nz/=nl;let vx=__P.vel.x,vz=__P.vel.z,vl=Math.hypot(vx,vz)||1;vx/=vl;vz/=vl;const py=__PLAYER().rotation.y,px=Math.sin(py),pz=Math.cos(py);return {yaw:__P.yaw,nx,nz,vx,vz,noseDot:nx*vx+nz*vz,riderDot:px*vx+pz*vz};})()\`);await key(cdp.evaluate,'KeyD',false);await key(cdp.evaluate,'KeyW',false);await sleep(250);const yawDelta=Math.atan2(Math.sin(turn.yaw-turnYaw0),Math.cos(turn.yaw-turnYaw0));assert(Math.abs(yawDelta)>0.25,'moving turn yaw delta '+yawDelta);assert(turn.noseDot>0.90,'moving turn camel nose dot '+turn.noseDot);assert(turn.riderDot>0.88,'moving turn Pling dot '+turn.riderDot);results.camel.turning={...turn,yawDelta};`, 'moving turn proof');
rep("await waitEval(cdp.evaluate,`__P.grounded===true`,5000);const y0=(await state(cdp.evaluate)).y;await key(cdp.evaluate,'Space',true);await sleep(70);const js=await state(cdp.evaluate);await key(cdp.evaluate,'Space',false);assert(js.camel&&js.vy>7&&js.y>y0,`mounted camel jump regressed vy=${js.vy} dy=${js.y-y0}`);results.camelJump={vy:js.vy,dy:js.y-y0};await sleep(900);",`await waitEval(cdp.evaluate,\`__P.grounded===true\`,5000);const jump0=await state(cdp.evaluate);await nativeSpace(true);const jumpSamples=[];for(let i=0;i<8;i++){await sleep(25);jumpSamples.push(await state(cdp.evaluate));}await nativeSpace(false);for(let i=0;i<6;i++){await sleep(25);jumpSamples.push(await state(cdp.evaluate));}const maxVy=Math.max(...jumpSamples.map(x=>x.vy)),maxY=Math.max(...jumpSamples.map(x=>x.y));assert(jumpSamples.every(x=>x.camel),'mounted camel jump lost mount');assert(maxVy>7&&maxY-jump0.y>0.15,\`mounted camel jump regressed maxVy=\${maxVy} dy=\${maxY-jump0.y}\`);results.camelJump={maxVy,dy:maxY-jump0.y};await sleep(900);`, 'mounted jump sampling');
rep("await driveTo(cdp.evaluate,0,-22,'ordinary quicksand lip');await jumpForward(cdp.evaluate,1050);let s=await state(cdp.evaluate);assert(!s.won&&!s.finish&&s.rec===0,'first ordinary quicksand incorrectly won/recovered during successful jump');",`await driveTo(cdp.evaluate,0,-24,'ordinary quicksand lip');const qHp0=await cdp.evaluate('__P.hp');await cameraForward(cdp.evaluate);await key(cdp.evaluate,'KeyW',true);await waitEval(cdp.evaluate,\`__P.quicksandRecT>0\`,3000);await key(cdp.evaluate,'KeyW',false);const qStart=await state(cdp.evaluate);assert(!qStart.won&&!qStart.finish&&qStart.rec>0,'ordinary quicksand did not enter recovery without winning');await waitEval(cdp.evaluate,\`__P.quicksandRecT===0&&__P.grounded===true\`,4000);const qEnd=await state(cdp.evaluate),qHp1=await cdp.evaluate('__P.hp');const qAnchor=await cdp.evaluate('({x:__P.safeAnchor.x,y:__P.safeAnchor.y,z:__P.safeAnchor.z})');assert(qHp1===qHp0-1,'ordinary quicksand did not cost exactly one heart');assert(!qEnd.won&&!qEnd.finish&&!qEnd.camel,'ordinary quicksand recovery state regressed');assert(Math.hypot(qEnd.x-qAnchor.x,qEnd.z-qAnchor.z)<0.2,'ordinary quicksand did not return to checkpoint');results.ordinaryQuicksand={hpBefore:qHp0,hpAfter:qHp1,start:qStart,end:qEnd,anchor:qAnchor};await driveTo(cdp.evaluate,4.8,-23,'recovery side route 1');await driveTo(cdp.evaluate,4.8,-32,'recovery side route 2');await driveTo(cdp.evaluate,3,-38,'second camel after recovery');await tap(cdp.evaluate,'Space');await waitEval(cdp.evaluate,\`!!__P.camel\`);let s=await state(cdp.evaluate);`, 'ordinary quicksand natural recovery');
rep("for(let i=0;i<5;i++){await tap(cdp.evaluate,'Space',50);await sleep(280);}","for(let i=0;i<5;i++){await nativeTapSpace(50);await sleep(280);}",'climb attack jumps');
rep("finally{if(cdp)cdp.close();", "finally{activeCDP=null;if(cdp)cdp.close();", 'CDP cleanup');
fs.writeFileSync(file,s);
console.log('LEVEL5_BROWSER_NATIVE_PATCH=APPLIED');
